#ifndef _ROLES_
#define _ROLES_

#include <filesystem>
#include <string>
#include <map>
#include <vector>

namespace svr {
	enum {
		RANK_PLAYER,
		RANK_MODERATOR,
		RANK_SUPER_MODERATOR
	};
}
#endif //! _ROLES_